// Define two numeric variables
const num1 = 5;
const num2 = 25;

// Set a new variable to determine if twice the first variable to determine if twice the first numeric variable is greater than the second numeric variable from above.
doubled = num1 * 2 > num2;

// Output the comparison
document.getElementById("math").innerHTML = doubled;

// Practice types and equality
document.getElementById("type").innerHTML = typeof (num1);